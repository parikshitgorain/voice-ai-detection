# Docker Environment Configuration
# Copy this to .env and update values

# API Key (REQUIRED - change this!)
VOICE_DETECT_API_KEY=your-secret-key-change-me

# CORS Origins (comma-separated)
CORS_ORIGINS=*

# Model paths (already mapped in docker-compose.yml)
DEEP_MODEL_PATH_ENGLISH=/app/backend/deep/multitask_English.pt
DEEP_MODEL_PATH_HINDI=/app/backend/deep/multitask_Hindi.pt
DEEP_MODEL_PATH_TAMIL=/app/backend/deep/multitask_Tamil.pt
DEEP_MODEL_PATH_MALAYALAM=/app/backend/deep/multitask_Malayalam.pt
DEEP_MODEL_PATH_TELUGU=/app/backend/deep/multitask_Telugu.pt

# Device selection
# auto = auto-detect GPU, fallback to CPU (recommended)
# cuda = force GPU (will fail if no GPU)
# cpu = force CPU
DEEP_MODEL_DEVICE=auto

# Queue settings
QUEUE_MAX_CONCURRENT=3
QUEUE_MAX_LENGTH=10

# Host and Port (usually don't need to change)
HOST=0.0.0.0
PORT=3000
NODE_ENV=production
