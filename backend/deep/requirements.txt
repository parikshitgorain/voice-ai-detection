# Deep model training requirements
# Install on training machine/VPS only.
# CPU only:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# CUDA (example for 12.1):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

torch>=2.2.0
torchaudio>=2.2.0
torchvision>=0.17.0
soundfile>=0.12.1
